class MessageService:
    """Normalize message persistence, retrieval, and tool transcript formatting."""

    def output_contract(self) -> str:
        return "messages persist normalized roles, content, and runtime metadata"
