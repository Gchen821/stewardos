# Reflection Controller

Filesystem-loaded control agent for the butler reflection stage.

This asset is not persisted as a database record.
It is loaded directly from `repositories/agents/reflection` during platform startup.
