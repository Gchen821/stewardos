# Planner Controller

Filesystem-loaded control agent for the butler planner stage.

This asset is not persisted as a database record.
It is loaded directly from `repositories/agents/planner` during platform startup.
